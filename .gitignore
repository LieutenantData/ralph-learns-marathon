# Ralph Extended - Sprint management files
# prd.json is generated by sprint.py and should not be committed
prd.json

# Progress tracking (session-specific)
scripts/ralph/progress.txt
scripts/ralph/.last-branch
scripts/ralph/archive/

# Keep backlog.json in git (it's your source of truth)
# !backlog.json
